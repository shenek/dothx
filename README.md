# Installation

Link the dot files::
    git clone --recursive git@github.com:shenek/dothx.git ~/helix
    cd ~/.config && ln -sf ../helix helix

# Useful commands

```
hx --health
```

```
hx --grammar fetch
```

```
hx --grammar build
```
